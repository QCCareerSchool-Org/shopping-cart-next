import type { FC } from 'react';
import type { School } from '@/domain/school';

type Props = {
  date: number;
  school: School;
};

export const VisualPaymentPlans: FC<Props> = ({ date, school }) => null;
